/**
 * Focuses the next or previous input-field in order.
 * @param event
 */
export const onFormKeydown = (event) => {
    const {key} = event
    switch (key) {
        case 'ArrowUp':
            break;
        case 'ArrowDown':
            break;
    }
}